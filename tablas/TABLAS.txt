USE EJERCICIO
GO
CREATE TABLE CLIENTE(
NOM VARCHAR(30) NOT NULL,
APES VARCHAR(30) NOT NULL,
CURP VARCHAR(18) NOT NULL,
DIR VARCHAR(30) NOT NULL,
TEL INT
PRIMARY KEY(CURP)
)
CREATE TABLE COMPRA(
CURP VARCHAR(18) NOT NULL,
COD_P INT
)
CREATE TABLE PRODUCTO(
NOMP VARCHAR(30) NOT NULL,
COD_P INT,
PRECIO FLOAT,
RFC_PRO VARCHAR(13) NOT NULL
PRIMARY KEY(COD_P)
)
CREATE TABLE PROVEEDOR(
RFC_PROO VARCHAR(13) NOT NULL,
NOM_PRO VARCHAR(30) NOT NULL,
DIR_PRO VARCHAR(30) NOT NULL,
PRIMARY KEY(RRC_PRO)
)
ALTER TABLE COMPRA
ADD FOREIGN KEY (CURP) REFERENCES CLENTE(CURP)
ALTER TABLE COMPRA
ADD FOREIGN KEY (COD_P) REFERENCES PRODUCTO(COD_P)
ALTER TABLE PRODUCTO
ADD FOREIGN KEY (RFC_PRO) REFERENCES PROVEEDOR(RFC_PRO)